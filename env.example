# Environment files

Use the split examples:

- Frontend: `frontend/env.example` → `frontend/.env`
- Backend: `backend/env.example` → `backend/.env`

A single root `.env` is still loaded by the backend if present.
